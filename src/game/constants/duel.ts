// Fixed logical arena size — identical on every screen.
// Phaser scales the canvas visually via Scale.FIT,
// but all in-game coordinates are always in this 0–W / 0–H space.
export const ARENA_W = 320
export const ARENA_H = 520
